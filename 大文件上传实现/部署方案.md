# 🚀 项目部署方案（无需自己的服务器）

## 📋 部署方案概览

即使没有自己的服务器，也有很多方案可以部署您的大文件上传系统：

| 方案 | 前端 | 后端 | 数据库 | 费用 | 推荐度 |
|------|------|------|--------|------|--------|
| Vercel + Railway | ✅ | ✅ | ✅ | 免费 | ⭐⭐⭐⭐⭐ |
| Netlify + Render | ✅ | ✅ | ✅ | 免费 | ⭐⭐⭐⭐⭐ |
| GitHub Pages + Railway | ✅ | ✅ | ✅ | 免费 | ⭐⭐⭐⭐ |
| 腾讯云轻量应用服务器 | ✅ | ✅ | ✅ | ¥50/月 | ⭐⭐⭐⭐⭐ |
| 阿里云云服务器ECS | ✅ | ✅ | ✅ | ¥70/月 | ⭐⭐⭐⭐⭐ |

---

## 🆓 方案一：Vercel + Railway（完全免费，推荐）

### 特点
- ✅ 完全免费
- ✅ 自动部署
- ✅ 全球CDN
- ✅ 支持自定义域名
- ⚠️ Railway每月500小时免费额度

### 部署步骤

#### 1. 前端部署到 Vercel

**步骤1：准备前端代码**
```bash
# React版本
cd 大文件上传实现/React版本

# 修改API地址为Railway后端地址（部署后获取）
# 在 vite.config.ts 中修改
```

**步骤2：部署到Vercel**
```bash
# 安装Vercel CLI
npm install -g vercel

# 登录Vercel
vercel login

# 部署
vercel

# 生产环境部署
vercel --prod
```

或者通过网页部署：
1. 访问 https://vercel.com
2. 点击 "Import Project"
3. 连接GitHub仓库
4. 选择 `React版本` 目录
5. 自动部署完成

**步骤3：配置环境变量**
```bash
# 在Vercel项目设置中添加
VITE_API_BASE_URL=https://your-railway-backend.up.railway.app/api
```

#### 2. 后端部署到 Railway

**步骤1：准备后端代码**
```bash
cd 大文件上传实现/backend

# 添加 railway.json 配置文件
```

创建 `railway.json`:
```json
{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS"
  },
  "deploy": {
    "startCommand": "npm run start:new",
    "healthcheckPath": "/api/health",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10
  }
}
```

**步骤2：创建Railway项目**
1. 访问 https://railway.app
2. 使用GitHub登录
3. 点击 "New Project"
4. 选择 "Deploy from GitHub repo"
5. 选择您的仓库和 `backend` 目录

**步骤3：添加MySQL数据库**
1. 在Railway项目中点击 "New"
2. 选择 "Database" → "MySQL"
3. 自动创建并获取连接信息

**步骤4：配置环境变量**
在Railway项目设置中添加：
```env
NODE_ENV=production
PORT=3000
DATABASE_TYPE=mysql

# Railway会自动注入这些MySQL变量
# MYSQLHOST
# MYSQLPORT
# MYSQLUSER
# MYSQLPASSWORD
# MYSQLDATABASE

# 或手动配置
DB_HOST=${{MYSQLHOST}}
DB_PORT=${{MYSQLPORT}}
DB_USER=${{MYSQLUSER}}
DB_PASSWORD=${{MYSQLPASSWORD}}
DB_NAME=${{MYSQLDATABASE}}

CORS_ORIGIN=https://your-vercel-app.vercel.app
```

**步骤5：部署**
- Railway会自动部署
- 获取部署URL：`https://your-app.up.railway.app`

---

## 🌐 方案二：Netlify + Render（完全免费）

### 特点
- ✅ 完全免费
- ✅ 自动部署
- ✅ 支持自定义域名
- ⚠️ Render免费版会休眠（15分钟无请求后）

### 部署步骤

#### 1. 前端部署到 Netlify

**步骤1：安装Netlify CLI**
```bash
npm install -g netlify-cli

# 登录
netlify login

# 部署
cd 大文件上传实现/React版本
netlify deploy

# 生产环境部署
netlify deploy --prod
```

或通过网页：
1. 访问 https://netlify.com
2. 拖拽 `React版本/dist` 目录（先执行 `npm run build`）
3. 自动部署完成

#### 2. 后端部署到 Render

1. 访问 https://render.com
2. 点击 "New +" → "Web Service"
3. 连接GitHub仓库
4. 配置：
   - **Root Directory**: `backend`
   - **Build Command**: `npm install`
   - **Start Command**: `npm run start:new`
   - **Environment**: Node

5. 添加环境变量（同Railway）

6. 添加MySQL数据库：
   - 点击 "New +" → "PostgreSQL"（或使用外部MySQL）

---

## 📦 方案三：GitHub Pages + Serverless

### 特点
- ✅ 前端完全免费
- ✅ 后端使用Serverless（按需付费）
- ⚠️ 需要改造后端为Serverless函数

### 部署步骤

#### 1. 前端部署到 GitHub Pages

```bash
cd 大文件上传实现/React版本

# 安装gh-pages
npm install -D gh-pages

# 在package.json中添加
{
  "scripts": {
    "deploy": "npm run build && gh-pages -d dist"
  },
  "homepage": "https://yourusername.github.io/your-repo-name"
}

# 部署
npm run deploy
```

#### 2. 后端使用 Vercel Serverless Functions

创建 `backend/api/upload.js`:
```javascript
// Vercel Serverless Function
export default async function handler(req, res) {
  // 上传逻辑
  if (req.method === 'POST') {
    // 处理上传
    res.status(200).json({ success: true });
  }
}
```

---

## ☁️ 方案四：腾讯云轻量应用服务器（¥50/月，推荐）

### 特点
- ✅ 性能稳定
- ✅ 国内访问快
- ✅ 完全控制
- ✅ 包含数据库
- 💰 新用户有优惠

### 部署步骤

#### 1. 购买服务器
1. 访问 https://cloud.tencent.com
2. 选择 "轻量应用服务器"
3. 配置：
   - **地域**: 选择离用户近的
   - **镜像**: Ubuntu 20.04
   - **套餐**: 2核2G（¥50/月）

#### 2. 安装环境
```bash
# SSH连接服务器
ssh root@your-server-ip

# 安装Node.js
curl -fsSL https://deb.nodesource.com/setup_18.x | sudo -E bash -
sudo apt-get install -y nodejs

# 安装MySQL
sudo apt-get install mysql-server

# 安装Nginx
sudo apt-get install nginx

# 安装PM2（进程管理）
npm install -g pm2
```

#### 3. 部署后端
```bash
# 上传代码到服务器
scp -r backend root@your-server-ip:/var/www/

# 在服务器上
cd /var/www/backend
npm install
pm2 start server-new.js --name upload-backend
pm2 save
pm2 startup
```

#### 4. 配置Nginx
```nginx
# /etc/nginx/sites-available/upload
server {
    listen 80;
    server_name your-domain.com;

    # 前端
    location / {
        root /var/www/frontend/dist;
        try_files $uri $uri/ /index.html;
    }

    # 后端API
    location /api {
        proxy_pass http://localhost:3000;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host $host;
        proxy_cache_bypass $http_upgrade;
        
        # 上传配置
        client_max_body_size 100M;
    }
}
```

```bash
# 启用配置
sudo ln -s /etc/nginx/sites-available/upload /etc/nginx/sites-enabled/
sudo nginx -t
sudo systemctl reload nginx
```

#### 5. 配置SSL（免费HTTPS）
```bash
# 安装Certbot
sudo apt-get install certbot python3-certbot-nginx

# 获取证书
sudo certbot --nginx -d your-domain.com

# 自动续期
sudo certbot renew --dry-run
```

#### 6. 部署前端
```bash
# 本地构建
cd React版本
npm run build

# 上传到服务器
scp -r dist root@your-server-ip:/var/www/frontend/
```

---

## 🐳 方案五：Docker部署（适合任何平台）

### 创建 Dockerfile

**后端 Dockerfile**:
```dockerfile
# backend/Dockerfile
FROM node:18-alpine

WORKDIR /app

COPY package*.json ./
RUN npm install --production

COPY . .

EXPOSE 3000

CMD ["npm", "run", "start:new"]
```

**前端 Dockerfile**:
```dockerfile
# React版本/Dockerfile
FROM node:18-alpine as build

WORKDIR /app
COPY package*.json ./
RUN npm install
COPY . .
RUN npm run build

FROM nginx:alpine
COPY --from=build /app/dist /usr/share/nginx/html
COPY nginx.conf /etc/nginx/conf.d/default.conf
EXPOSE 80
CMD ["nginx", "-g", "daemon off;"]
```

### Docker Compose
```yaml
# docker-compose.yml
version: '3.8'

services:
  frontend:
    build: ./React版本
    ports:
      - "80:80"
    depends_on:
      - backend

  backend:
    build: ./backend
    ports:
      - "3000:3000"
    environment:
      - DATABASE_TYPE=mysql
      - DB_HOST=mysql
      - DB_PORT=3306
      - DB_USER=root
      - DB_PASSWORD=password
      - DB_NAME=upload_system
    depends_on:
      - mysql

  mysql:
    image: mysql:8
    environment:
      MYSQL_ROOT_PASSWORD: password
      MYSQL_DATABASE: upload_system
    volumes:
      - mysql_data:/var/lib/mysql
      - ./backend/init-database.sql:/docker-entrypoint-initdb.d/init.sql

volumes:
  mysql_data:
```

### 部署到任何支持Docker的平台
```bash
# 本地测试
docker-compose up -d

# 部署到平台（如Railway、Render等都支持Docker）
```

---

## 🌟 推荐方案对比

### 个人学习/演示项目
**推荐**: Vercel + Railway（完全免费）
- 前端: Vercel
- 后端: Railway
- 数据库: Railway MySQL
- 总成本: ¥0/月

### 小型商业项目
**推荐**: 腾讯云轻量服务器
- 服务器: 2核2G
- 包含: 前端+后端+数据库
- 总成本: ¥50/月

### 中型项目
**推荐**: 阿里云/腾讯云
- 服务器: 2核4G
- 对象存储: OSS/COS
- CDN加速
- 总成本: ¥150-300/月

---

## 📝 部署前准备

### 1. 修改配置文件

**React版本 - vite.config.ts**:
```typescript
export default defineConfig({
  // ... 其他配置
  server: {
    proxy: {
      '/api': {
        target: process.env.VITE_API_BASE_URL || 'http://localhost:3000',
        changeOrigin: true
      }
    }
  }
})
```

**创建环境变量文件**:
```bash
# React版本/.env.production
VITE_API_BASE_URL=https://your-backend-url.com/api
```

### 2. 构建项目

```bash
# React版本
cd React版本
npm run build
# 产物在 dist 目录

# Vue3版本
cd Vue3版本
npm run build
# 产物在 dist 目录
```

### 3. 后端准备

**修改 CORS 配置**:
```javascript
// backend/src/config/index.js
cors: {
  origin: process.env.CORS_ORIGIN || '*',
  credentials: true
}
```

**环境变量**:
```bash
# 生产环境 .env
NODE_ENV=production
PORT=3000
CORS_ORIGIN=https://your-frontend-domain.com
DATABASE_TYPE=mysql
# ... 其他配置
```

---

## 🚀 快速开始（5分钟部署）

### 使用 Vercel + Railway

```bash
# 1. 部署前端到 Vercel
cd React版本
vercel --prod

# 2. 部署后端到 Railway
# 访问 railway.app，连接GitHub仓库

# 3. 在Railway添加MySQL数据库

# 4. 配置环境变量

# 5. 完成！访问 Vercel 提供的域名
```

---

## 🔧 域名配置（可选）

### 购买域名
- 阿里云: https://wanwang.aliyun.com （¥9/年起）
- 腾讯云: https://dnspod.cloud.tencent.com （¥8/年起）
- GoDaddy: https://godaddy.com （国际域名）

### 配置DNS
```
# A记录（如果用自己服务器）
@    A    your-server-ip
www  A    your-server-ip

# CNAME记录（如果用Vercel/Netlify）
@    CNAME    your-app.vercel.app
www  CNAME    your-app.vercel.app
```

---

## 📊 成本对比

| 方案 | 月费用 | 年费用 | 适用场景 |
|------|--------|--------|---------|
| Vercel + Railway | ¥0 | ¥0 | 学习、演示 |
| Netlify + Render | ¥0 | ¥0 | 学习、演示 |
| 腾讯云轻量 | ¥50 | ¥600 | 小型商业 |
| 阿里云ECS | ¥70 | ¥840 | 中型商业 |
| + 域名 | ¥1 | ¥10 | 所有 |
| + CDN | ¥20 | ¥240 | 加速访问 |

---

## ⚡ 部署检查清单

### 部署前
- [ ] 修改API地址配置
- [ ] 构建前端项目
- [ ] 配置环境变量
- [ ] 准备数据库初始化脚本
- [ ] 测试CORS配置

### 部署中
- [ ] 上传代码到服务器/平台
- [ ] 安装依赖
- [ ] 初始化数据库
- [ ] 配置环境变量
- [ ] 启动服务

### 部署后
- [ ] 测试上传功能
- [ ] 检查数据库连接
- [ ] 验证文件存储
- [ ] 测试断点续传
- [ ] 配置SSL证书（HTTPS）
- [ ] 设置域名（可选）

---

## 🆘 常见问题

### Q1: 免费方案有什么限制？
**A**: 
- Railway: 每月500小时免费
- Vercel: 100GB带宽/月
- Render: 服务会休眠
- 建议：测试和学习够用

### Q2: 如何选择部署方案？
**A**:
- **学习/演示**: Vercel + Railway（免费）
- **个人项目**: 腾讯云轻量（¥50/月）
- **商业项目**: 阿里云/腾讯云（根据需求）

### Q3: 数据库如何迁移？
**A**:
```bash
# 导出数据
mysqldump -u root -p upload_system > backup.sql

# 导入到新数据库
mysql -u root -p new_database < backup.sql
```

### Q4: 如何监控服务？
**A**:
- Railway/Render自带监控
- 自建服务器使用PM2: `pm2 monit`
- 配置日志: `pm2 logs`

---

## 📚 推荐阅读

- [Vercel文档](https://vercel.com/docs)
- [Railway文档](https://docs.railway.app)
- [Netlify文档](https://docs.netlify.com)
- [腾讯云文档](https://cloud.tencent.com/document)
- [Nginx配置](https://nginx.org/en/docs/)

---

**开始部署**: 根据预算和需求选择方案，跟着步骤操作即可！🚀

**需要帮助**: 随时询问，我会协助您完成部署！ 